"""Media URL utilities.

Pure helpers for building media URLs from relative paths stored in the DB.
The base URL comes from configuration so the same function works in dev,
staging and prod without code changes.
"""
