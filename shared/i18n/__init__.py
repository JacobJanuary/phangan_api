"""i18n translation store — loads UI strings from `app_i18n` (Postgres).

Entries are keyed by `(language, screen, key)` and exposed grouped by screen
to match the frontend's expected response shape.
"""
