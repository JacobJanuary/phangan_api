"""Vertical-slice feature modules.

Each subpackage is a self-contained feature with its own routes, services,
repository, and schemas. Cross-feature contracts go through `core.*` and
`shared.*` only.
"""
